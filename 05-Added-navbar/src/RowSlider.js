import React from "react";
import classes from "./RowSlider.module.css";

function RowSlider(props) {
  return;
  <div>
    <img src={props.source} alt={props.alt} />
  </div>;
}

export default RowSlider;
